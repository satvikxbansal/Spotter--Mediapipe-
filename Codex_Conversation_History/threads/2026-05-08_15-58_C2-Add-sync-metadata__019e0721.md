# C2: Add sync metadata

- Thread ID: 019e0721-ed38-75e0-a342-d462c36806ad
- Created: 2026-05-08 15:58:44 (Asia/Kolkata)
- Updated: 2026-05-08 16:13:22 (Asia/Kolkata)
- Archived: no
- Workspace: /Users/satvik.bansal/Desktop/VirtualTrainer - mediapipe
- Source rollout: /Users/satvik.bansal/.codex/sessions/2026/05/08/rollout-2026-05-08T15-58-44-019e0721-ed38-75e0-a342-d462c36806ad.jsonl
- Git branch at thread start: main
- Git SHA at thread start: f8950e317f945531e56e159a459d7551b4b6581f
- Exported visible messages: 14
- Extracted input images: 0

## Conversation

### User - 2026-05-08 15:59:06

You are working in the Spotter iOS Swift repository.

Before changing code:
1. Deeply inspect the current repository.
2. Read the current README and DEBUG_LOG.md.
3. Read relevant files before editing.
4. Treat this prompt as implementation guidance, not a blind spec. If the current code reveals a safer approach, choose it and explain why.
5. Do not rewrite MediaPipe, CameraManager, PoseEstimator, UniversalRepCounter, FormFeedbackEngine, HandGestureDetector, ExertionAnalyzer, or the live camera pipeline.
6. Preserve both live flows:
   - Camera tab free analysis
   - Planned workout sessions
7. Preserve deterministic local plan generation, trophies, stats, trends, recaps, and AI insights.
8. Keep backend work local-first until the Firebase phase explicitly begins.
9. Do not store or upload raw video, camera frames, face images, raw pose streams, raw biometric face data, or raw pose timelines.
10. Do not print or expose secret values.
11. Maintain backwards-compatible Codable decoding for existing local JSON.
12. Keep the app compiling after this phase.
13. Run available tests/build commands and summarize results.
14. Summarize changed files, key decisions, migration behavior, and known follow-ups.
15. Mention the change the code brings to the application - the before and after with real life practical examples if possible. 
Use the common Spotter Codex preflight block.

Goal:
Add sync metadata to every future-syncable entity before repository protocols exist.

Create:
- SyncMetadata.swift

Models:
enum SyncState: String, Codable {
    case localOnly
    case pendingUpload
    case synced
    case conflict
}

struct SyncMetadata: Codable, Equatable {
    var localUpdatedAt: Date
    var lastSyncedAt: Date?
    var serverVersion: String?
    var syncState: SyncState
    var pendingOperationId: UUID?

    static func initialLocalOnly(now: Date = Date()) -> SyncMetadata
    static func initialPendingUpload(operationId: UUID?, now: Date = Date()) -> SyncMetadata
}

Add syncMetadata to:
- UserProfile
- WorkoutSessionSummary
- TrophyProgress
- TrophyUnlockEvent
- AIInsight
- InsightDeliveryRecord
- InsightEngagementRecord
- CalibrationRecord
- Theme envelope if persisted independently

Rules:
- Missing syncMetadata decodes to .initialLocalOnly.
- In local-only mode, new records can remain .localOnly.
- When accountId exists or a repository save occurs, local mutations should mark .pendingUpload.
- A server repository will later set .synced with lastSyncedAt and serverVersion.
- Conflicts should be marked .conflict, not silently overwritten.

Add:
Documentation/SyncConflictResolution.md

Document:
- UserProfile: last-write-wins by localUpdatedAt, but preserve createdAt and onboardingCompletedAt.
- WorkoutSessionSummary: immutable after sync except tombstone delete.
- TrophyProgress: derived; canonical unlocks come from TrophyUnlockEvent log.
- TrophyUnlockEvent: append-only, earliest server timestamp wins.
- AIInsight: latest sourcePolicyVersion wins by dedupeKey; tombstoned evidence invalidates.
- InsightDeliveryRecord: merge by max lastPresentedAt per surface and sum count where appropriate.
- InsightEngagementRecord: merge counts and max last engagement date.
- CalibrationRecord: completed beats skipped/failed; otherwise latest wins.
- Theme: last-write-wins.

Tests:
- missing syncMetadata decodes
- mutation marks pendingUpload when accountId exists
- localOnly records remain localOnly in no-account mode
- conflict policy doc exists
- SyncMetadata Codable roundtrip

Acceptance:
- Sync state exists before repositories.
- Conflict rules are explicit.

### Assistant - 2026-05-08 15:59:17

I’ll start with the Spotter preflight: map the repo, read the README and debug log, then trace the model/persistence paths that touch these entities before making any edits. After that I’ll implement the smallest compatible metadata layer and verify it with the available build/tests.

### Assistant - 2026-05-08 15:59:26

The worktree is clean, and this is a compact iOS repo with the syncable entities mostly under `VirtualTrainer/Models`. I’m going to read the project notes plus search for any existing “preflight” wording and account-save patterns so the sync flags land where the app already persists state.

### Assistant - 2026-05-08 15:59:57

The readiness map is directly relevant here: the repo already has account ownership and tombstones, but intentionally had no `SyncMetadata` yet. That means the safest path is to extend the existing local JSON models and store save points, not introduce repository abstractions or touch the camera/tracking stack.

### Assistant - 2026-05-08 16:02:13

I’ve got the shape now. Plan is: add a tiny sync metadata model and helper, thread it through the syncable Codable entities with legacy defaults, stamp pending state only at local mutation/save points where an account is present, add the conflict-resolution doc, then cover the behavior with focused XCTest cases before running the workspace tests/build.

### Assistant - 2026-05-08 16:02:29

I’m going to make the metadata helper preserve explicit `.conflict` states once they exist. That’s a small guardrail now, but it matters later: normal save paths shouldn’t accidentally turn a conflict into a pending upload without a resolver choosing a side.

### Assistant - 2026-05-08 16:07:53

The model layer is wired now. I’m adding focused tests that use the real stores where the behavior matters, plus direct Codable round trips so legacy JSON without `syncMetadata` stays safe.

### Assistant - 2026-05-08 16:09:21

The tests are in place. I’m going to run the focused XCTest class first because the Codable synthesis around these value types is the likeliest place for Swift to object; after that I’ll run the broader workspace test/build path.

### Assistant - 2026-05-08 16:09:45

Focused build found one straightforward Swift issue: `InsightEngagementRecord.withAccountId` now has a local binding, so it needs an explicit `return`. I’m fixing that and rerunning the focused test.

### Assistant - 2026-05-08 16:10:41

The focused test build is past the compile fixes and into simulator execution now. I’m waiting on the test runner result rather than interrupting it.

### Assistant - 2026-05-08 16:11:15

Focused sync tests pass. I’m moving to the full workspace test suite now; that’s the real check that the metadata defaults didn’t disturb history, trophies, insights, onboarding, or the planned/free-analysis save paths.

### Assistant - 2026-05-08 16:11:50

Full tests are running on the simulator. The build phase is clean so far; now I’m watching the actual XCTest run for any behavioral fallout in older persistence tests.

### Assistant - 2026-05-08 16:12:15

Full tests passed. I’m doing a plain simulator build next, which catches app target packaging issues that tests can sometimes mask.

### Assistant - 2026-05-08 16:13:22

Implemented the sync-metadata prep, local-first and without touching MediaPipe, CameraManager, PoseEstimator, UniversalRepCounter, FormFeedbackEngine, HandGestureDetector, ExertionAnalyzer, or the live camera pipeline.

**What Changed**
- Added [SyncMetadata.swift](</Users/satvik.bansal/Desktop/VirtualTrainer - mediapipe/VirtualTrainer/Models/SyncMetadata.swift:3>) with `SyncState`, local-only and pending-upload initializers, mutation stamping, and conflict-preserving merge preference.
- Added `syncMetadata` with backward-compatible decoding to:
  [UserProfile.swift](</Users/satvik.bansal/Desktop/VirtualTrainer - mediapipe/VirtualTrainer/Models/UserProfile.swift:276>),
  [WorkoutSessionSummary.swift](</Users/satvik.bansal/Desktop/VirtualTrainer - mediapipe/VirtualTrainer/Models/WorkoutSessionSummary.swift:247>),
  [TrophyModels.swift](</Users/satvik.bansal/Desktop/VirtualTrainer - mediapipe/VirtualTrainer/Models/TrophyModels.swift:192>),
  [AIInsightModels.swift](</Users/satvik.bansal/Desktop/VirtualTrainer - mediapipe/VirtualTrainer/Models/AIInsightModels.swift:155>),
  [InsightStore.swift](</Users/satvik.bansal/Desktop/VirtualTrainer - mediapipe/VirtualTrainer/Models/InsightStore.swift:8>),
  [CalibrationRecord.swift](</Users/satvik.bansal/Desktop/VirtualTrainer - mediapipe/VirtualTrainer/Models/CalibrationRecord.swift:41>), and the persisted theme envelope in [ThemeStore.swift](</Users/satvik.bansal/Desktop/VirtualTrainer - mediapipe/VirtualTrainer/Models/ThemeStore.swift:158>).
- Store saves now mark account-owned local mutations as `.pendingUpload`; no-account/local-only saves remain `.localOnly`.
- `WorkoutHistoryStore.fetchDirtyOrDeletedSummaries()` now includes pending/conflict sync records as well as tombstones.
- Added [SyncConflictResolution.md](</Users/satvik.bansal/Desktop/VirtualTrainer - mediapipe/Documentation/SyncConflictResolution.md:1>) with explicit merge/conflict rules.
- Added [SyncMetadataTests.swift](</Users/satvik.bansal/Desktop/VirtualTrainer - mediapipe/VirtualTrainerTests/SyncMetadataTests.swift:9>) covering Codable roundtrip, legacy missing metadata, pending upload stamping, local-only behavior, and the conflict doc.

**Practical Before/After**
Before: if a signed-in user saved a workout or changed their goal, the local JSON had ownership but no way for a future sync layer to know “this needs upload.”  
After: that same profile/workout/insight/theme/calibration/trophy state is marked `.pendingUpload`. A no-account user doing Free Analysis still saves local history as `.localOnly`, so local-first behavior stays quiet and offline.

**Migration**
Existing JSON without `syncMetadata` decodes as `.initialLocalOnly`, even if it already has `accountId`. That keeps old local files safe and avoids pretending legacy data is dirty.

**Verification**
- `xcodebuild test -workspace VirtualTrainer.xcworkspace -scheme VirtualTrainer -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:VirtualTrainerTests/SyncMetadataTests` succeeded.
- `xcodebuild test -workspace VirtualTrainer.xcworkspace -scheme VirtualTrainer -destination 'platform=iOS Simulator,name=iPhone 17'` succeeded.
- `xcodebuild build -workspace VirtualTrainer.xcworkspace -scheme VirtualTrainer -destination 'platform=iOS Simulator,name=iPhone 17'` succeeded.
- `git diff --check` clean.

Known follow-ups: operation IDs are still nil until the idempotency/write-journal phase, and server repositories still need to set `.synced`, `lastSyncedAt`, and `serverVersion` later.
